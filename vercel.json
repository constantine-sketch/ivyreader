{
  "version": 2,
  "cleanUrls": true,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/trpc/(.*)",
      "dest": "/api/trpc/[trpc].ts"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
